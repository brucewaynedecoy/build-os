# Agent Instructions

When asked to create documentation for this project that is not `README.md`, read the same-named instruction file in `docs/` before writing.

## Operating the system

For operating-the-system routing, read [`AGENTS.md`](AGENTS.md) in this directory and follow it
(it is the single source of truth for our routing; it points into [`.os/`](.os/AGENTS.md)).
